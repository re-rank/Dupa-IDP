-- Add branch column to projects table (if not exists)
-- SQLite doesn't support IF NOT EXISTS for ALTER TABLE, so we'll handle this in code

-- This migration is handled by the schema.sql file now
-- No action needed as columns are already in the main schema